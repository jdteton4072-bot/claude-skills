# RAG Architecture Memo — NFL/CFB Spread & Totals Prediction Service

**Author role:** Retrieval-systems architecture (supporting role to edge research)
**Date:** 2026-09-05 (corrected 2026-09-05 following citation audit — see Correction Log)

---

## Recommendation

- **Recommend (c), narrowly scoped, and built almost entirely on top of (b):** a classical statistical/ML core (gradient-boosted trees or a hierarchical Bayesian model, consistent with the team's existing HMM/walk-forward-validation competence) **emits every published number**. Retrieval and LLMs appear in exactly two bounded, auditable roles: (1) **offline feature extraction** — turning unstructured injury/beat-reporter/news text into small, versioned numeric/categorical features that are inputs to the stats model, and (2) a **narrow near-real-time monitoring layer** that flags when a late-breaking unstructured signal (a scratch, a weather shift) has occurred *after* the model's last feature refresh and *before* the line has moved to reflect it — surfaced to a human, never auto-emitting a new number. **RAG-as-predictor (option a) is rejected** for the core prediction task. Confidence: Medium-High.
- **Why:** LLMs are demonstrated to be extraction/classification tools, not calibrated numeric forecasters (see §1). The 52.38% breakeven bar means the system needs a *reproducible, backtestable, low-variance* number generator; an LLM reasoning end-to-end to a spread reintroduces non-determinism, cost, and — critically — training-data contamination directly into the one output that decides whether the business survives. A stats model with LLM-derived features keeps the actual number-generation classical and testable, while still letting retrieval do the one thing it's plausibly good for: fast, structured extraction of information the market may not have fully priced.
- **Single biggest risk to this design:** **point-in-time leakage.** Any LLM in the pipeline — even doing "only" feature extraction — was pretrained on a corpus that already contains sports journalism, box scores, and post-game analysis for most historical games used in backtesting. If retrieval and feature extraction are not strictly bound to an as-of-timestamp index (§2), the backtest will look better than the live system ever will, and the team will not find out until real money is down. This is treated as a first-class constraint below, not a caveat.

---

## 1. Resolving the definition

### (a) RAG-as-predictor
**Mechanism:** LLM retrieves stats/injury/news/line context at inference time and reasons directly to a spread, total, or pick.

**Plausible edge:** In principle, an LLM could integrate qualitative signals (a backup QB's specific weaknesses, a beat reporter's off-the-record read on a coach's game plan) that a discretized stats model handles crudely. No evidence found that this integration step, in practice, produces calibrated numeric output rather than plausible-sounding narrative.

**Evidence on numeric calibration:** The best available proxy evidence is LLM forecasting-tournament literature, not point-spread-specific research (that gap is real and is flagged as such — no dedicated study of LLM-generated point spreads or totals calibration was found in this search). ForecastBench ([Karger, Bastani, et al., arXiv:2409.19839, submitted Sep 2024, published as a conference paper at ICLR 2025](https://arxiv.org/abs/2409.19839)) evaluated 17 LLMs (including GPT-4o, Claude 3.5 Sonnet, Gemini 1.5 Pro) against human forecasters on real-world, previously-unresolved questions drawn from prediction markets and public datasets (Metaculus, Manifold, Polymarket, ACLED, FRED, Wikipedia, among others), and reported Brier scores of **0.093 for expert superforecasters, 0.107 for the general public crowd, and 0.111 for the best-performing LLM tested (Claude 3.5 Sonnet)** — i.e., the best LLM tested in that benchmark underperformed *both* superforecasters and an ordinary crowd, not merely the superforecasters.

*Verification note:* direct WebFetch retrieval of arxiv.org was blocked by this environment's network egress policy on every attempt in this correction round. These specific figures are corroborated identically across three independent WebSearch queries drawing on the arXiv abstract/PDF/HTML mirrors, an EA Forum announcement written by the paper's own research team, a ResearchGate mirror, and an ICLR 2025 slide deck. Treated as verified via cross-source triangulation, not a single direct primary-document read. **Confidence: Medium** (was previously miscited with fabricated figures — see Correction Log).

A separate, smaller, independent paper — [Future Is Unevenly Distributed: Forecasting Ability of LLMs Depends on What We're Asking (Karkar & Chopra, Lossfunk, arXiv:2511.18394, submitted Nov 23, 2025)](https://arxiv.org/abs/2511.18394) — reports that LLM forecasting competence is highly domain- and prompt-dependent and documents recurrent calibration failure modes that emerge specifically when contextual news is added to a forecasting prompt. This is a two-author, non-benchmark-consortium paper, not a large tournament result, and is cited here only for its qualitative finding (calibration degrades under added context, in ways that are relevant to a system that injects retrieved news into a forecasting prompt) — not for any specific Brier-score figure. Weighted lower than ForecastBench. **Confidence: Medium-Low.**

Whether later reasoning-oriented models (o3-class, GPT-5-class, or other 2025-2026 frontier models) have closed the ForecastBench gap was **not established** in this pass — no dated benchmark reporting comparable Brier-score figures for those specific models was located. **The calibration claim is therefore scoped strictly to the models ForecastBench actually tested (2024-era GPT-4o/Claude-3.5/Gemini-1.5 class)** and should not be read as a claim about current frontier models; extending it to today's models is judgment, not measurement.

This is evidence about *probability* calibration on discrete world events, not about emitting a calibrated *point-spread number* — extrapolating it to "an LLM cannot output a good spread" is **judgment**, but the direction of that judgment (structurally worse at precise numeric quantities than at classification/extraction, and in ForecastBench's case worse than even an ordinary human crowd) is consistent with this literature and with the general shape of LLM training objectives (next-token prediction over text, not squared-error minimization over a continuous target).

**Failure modes:** (i) training-data contamination — see below and §2, this is not hypothetical, it is close to guaranteed for any backtest touching historical games; (ii) hallucinated stats presented with false confidence; (iii) no native uncertainty quantification a bettor can size a stake against; (iv) non-determinism from model updates and batched-inference floating-point effects breaks the ability to reproduce a backtest at all (§1, determinism below); (v) an LLM asked to "reason to a number" is also the most expensive and highest-latency option per game.

**Cost/latency:** Highest of the three options — long-context retrieval, multi-call self-consistency needed to reduce variance, and the contamination-control machinery in §2 has to wrap the entire prediction path, not just a feature layer.

**Verdict:** Reject as the number-generating mechanism. Low confidence this ever clears 52.38%; no plausible mechanism beyond what a stats model already captures from EPA/success-rate/line data, and it actively adds contamination and reproducibility risk to the one output that matters.

### (b) RAG-as-feed
**Mechanism:** Retrieval assembles and normalizes features. Structured sources (play-by-play, lines, weather) are mostly **plain SQL/feature-store lookups, not retrieval in the RAG sense at all** (see §3 — flagged per-source). Unstructured sources (injury notes, beat reports, depth-chart chatter) are distilled by a classifier/small-LLM step into a bounded set of numeric/categorical features (e.g., `starting_qb_out: bool`, `practice_participation_trend: ordinal(0-3)`, `beat_reporter_severity_score: float[-1,1]` with a confidence field) that join the same feature vector as the structured data. A separate, classical model (GBM, or a hierarchical Bayesian model with team/season random effects — directly analogous to the team's HMM background) does all calibration and number emission, e.g., via isotonic regression or Platt scaling on top of the raw model output.

**Plausible edge:** Whatever edge exists plausibly lives in (i) the stats model's ability to weight and calibrate already-known signals better than the market's simplified consensus, and (ii) the marginal information in unstructured text that is genuinely not yet reflected in structured data or the line — e.g., a beat reporter's report of a limited practice participant trending toward "doubtful" a few hours before the official designation. The LLM's job is bounded to classification/extraction, which is the task class LLMs are actually good at relative to open-ended numeric generation.

**Failure modes:** Contamination is still possible at the feature-extraction step (an LLM summarizing a 2021 injury report may lean on memorized knowledge of how that season turned out); overfitting the stats model to noisy, LLM-derived features if extraction quality isn't independently validated; feature drift if extraction prompts/models change without version pinning.

**Cost/latency:** Low-medium. Feature extraction is batchable (nightly per slate), not a live reasoning call; the stats model itself is near-instant at inference.

**Verdict:** This is the dominant, load-bearing role for retrieval in this system.

### (c) Hybrid
**Mechanism:** Everything in (b), plus one additional, deliberately narrow real-time component: a **line-staleness monitor**. Between the time features were last refreshed and kickoff, an event-driven pipeline watches a small set of high-value unstructured triggers (official injury designation changes, a beat reporter reporting a scratch, a sudden wind-forecast revision) and computes a lightweight severity/direction score. This score does **not** generate a new predicted number; it flags "the model's last output is now stale relative to a real-world change, and the market may not have repriced yet" for human review or a narrow rule-based re-weighting (e.g., "QB out" triggers a pre-computed backup-QB adjustment already validated offline, not a fresh LLM-generated number).

**Plausible edge:** This is the only place in the architecture where *speed* itself is the edge candidate — beating the market's own repricing of a late-breaking event by minutes to hours. This is a timing/latency arbitrage, not a modeling insight, and its existence depends entirely on the market being slow to reprice public information, which the architecture cannot verify (see §6).

**Failure modes:** False triggers from unreliable beat-reporter sourcing; the "narrow rule-based re-weighting" must itself be pre-validated offline (not invented live) or it reintroduces the same calibration and contamination problems as option (a), just at smaller scale and higher frequency.

**Verdict:** Adopt as an *extension* of (b), not as a separate independent architecture. This is what "hybrid" concretely means here: a classical predictor, a batch LLM feature layer, and a bounded, human-gated, non-generative real-time alerting layer. At no point does an LLM emit the published spread or total.

### Training-data contamination — first-class constraint, not a caveat
**Mechanism:** Pretraining corpora for any general-purpose LLM include sports journalism, box scores, post-game recaps, and betting-market commentary through the model's training cutoff. If that LLM is used anywhere in the prediction path — even "only" to extract a feature like "how severe is this injury" — for a **historical** game used in backtesting, its output can be influenced by memorized knowledge of how that game turned out, not just by the as-of-date facts it was given. E.g., asked in 2026 to assess whether a 2022 backup QB start "will hurt performance," the model may lean toward the historically correct answer because it has seen post-game analysis describing the outcome, independent of what was actually knowable before kickoff.

**Severity:** This is not a minor caveat — it is the mechanism most likely to make a backtest look profitable when the live system is not. Recent literature on this exact failure mode: [Temporal Leakage in LLM Backtesting: Measurement, Validation, and Adjusted Scores, arXiv, Aug 2026](https://arxiv.org/html/2608.02985) and [All Leaks Count, Some Count More: Interpretable Temporal Contamination Detection and Mitigation, arXiv, Feb 2026](https://arxiv.org/html/2602.17234) — both establish that the naive check (compare pre-cutoff vs. post-cutoff performance) is *itself* unreliable, because models can fail that check even on questions they provably could not have memorized, meaning contamination detection needs the ablation and canary methods below, not a single before/after split. (These two citations were not flagged by the audit and are unchanged from the prior draft; they were not re-verified in this correction round beyond the original pass.)

**Architectural mitigations (mandatory, not optional):**
1. **Strict as-of-date retrieval** (§2) — the index itself, not a prompt instruction, enforces that only information timestamped before the decision point is retrievable.
2. **Post-cutoff holdout as the only valid backtest window for LLM-touched components.** Any pinned LLM has a public training cutoff; only games occurring after that cutoff can be used to validate the LLM-containing feature-extraction layer under conditions resembling live deployment. Pre-cutoff games can validate the *classical stats model* (which has no memorization channel) but cannot cleanly validate the LLM feature layer.
3. **Ablation testing:** run the identical stats-model backbone with the LLM-derived features present vs. removed, separately for pre-cutoff and post-cutoff games. A performance gain that appears only pre-cutoff and vanishes post-cutoff is the contamination signature.
4. **Canary/counterfactual injection:** insert deliberately fabricated facts (a fictitious injury, a false depth-chart change) into the as-of-date index for real historical games and confirm the LLM's derived feature reflects the injected counterfactual rather than the real, memorized outcome. If the model "corrects" back toward the real-world result, that is direct evidence of memorization overriding retrieved context.

Confidence in the *existence and severity* of this risk: High. Confidence in any specific mitigation being fully sufficient: Medium — this is an active research area (both cited papers are 2026) and the state of the art has not converged.

### Determinism and reproducibility
A backtest that cannot be rerun to the same result is not a backtest. Two distinct sources of non-determinism apply if an LLM is anywhere in the path:
- **Model-version drift.** Hosted model endpoints change silently; a backtest run in month 1 and reproduced in month 6 against "the same model name" may not be the same weights. **Mitigation:** pin exact model versions/checkpoints, and re-run and diff outputs whenever a provider deprecates a pinned version.
- **Batch-size-dependent floating-point non-determinism.** Even at temperature 0, identical prompts can return different completions because dynamic batching changes the internal order of floating-point reductions in GPU kernels ("batch invariance"). [Thinking Machines Lab, "Defeating Nondeterminism in LLM Inference," Sep 10, 2025](https://thinkingmachines.ai/blog/defeating-nondeterminism-in-llm-inference/) — the primary source for this finding — ran 1,000 greedy (temperature-0) completions of a fixed prompt on Qwen3-8B using standard, batch-variant kernels and found 80 of the 1,000 completions diverged from the modal answer (outputs identical through token 102, diverging at token 103); switching to batch-invariant kernels made all 1,000 completions identical, at a measured cost of roughly 1.6-2x more inference time in their benchmark. This is a concrete, primary-sourced demonstration that temperature=0 alone does not guarantee reproducibility. Corroborated by independent write-ups: [Simon Willison, Sep 11, 2025](https://simonwillison.net/2025/Sep/11/defeating-nondeterminism/); [Medium, "Defeating nondeterminism in LLM inference, in practice," 2025](https://medium.com/@siddhantg314/defeating-nondeterminism-in-llm-inference-in-practice-38a7dd1e4112). **Mitigation:** cache every LLM output keyed on (input hash, model version, prompt version); never regenerate a cached historical feature; if self-hosting, use batch-invariant inference kernels for anything that must be exactly reproducible.
- **Practical implication:** treat every LLM call in the pipeline as a **write-once artifact** — logged, hashed, versioned — exactly like a market-data snapshot, not as a function that can be silently recomputed later and assumed equivalent.

---

## 2. Point-in-time correctness — the central engineering constraint

This is where the design lives or dies, per the brief, and it is the section with the least tolerance for hand-waving.

### Bitemporal modeling
Every fact in the system carries **two** timestamps:
- **Event time** — when the fact became true in the world (e.g., a player suffered an injury, a line moved, wind speed at kickoff).
- **Knowledge time** — when the system's ingestion pipeline learned the fact and it became queryable (e.g., when a beat reporter's tweet was scraped, when a sportsbook's API returned the updated line).

A prediction for game G at decision timestamp T must only ever query facts where `knowledge_time <= T`, regardless of `event_time`. This is standard bitemporal data modeling (judgment: this is established practice in financial market-data and clinical-data systems generally, not something specific to this domain that needs a fresh citation — but the specific application to sports retrieval is this memo's judgment call, not a cited external practice).

Concretely: every row in every source table (injury reports, lines, weather, news, depth charts) gets `event_time`, `ingested_at` (knowledge time), and an immutable `source_snapshot_id`. Nothing is ever updated in place; corrections are new rows with a later `ingested_at`, and the original row is retained.

### Why "add a date filter to the prompt" fails
The common shortcut — retrieve broadly, then instruct the LLM "only use information before date X" — fails for two independent reasons:
1. **It relies on the index already excluding future information, and that's exactly the property it doesn't have.** If the underlying vector/document store contains post-game articles and box scores (because they were ingested in one big historical crawl), a semantic query like "QB injury Week 6 2023" can retrieve a chunk from a Week 7 recap that discusses how the injury affected the outcome — the date instruction only tells the model to *ignore* it, and compliance with that instruction is not guaranteed, verifiable, or enforceable at the retrieval layer.
2. **It does nothing about the model's parametric memory.** Even with a perfectly filtered retrieval set, the LLM's weights already encode the outcome if the game is pre-cutoff. Prompt-level date instructions cannot filter what's already baked into the model — only post-cutoff holdout and ablation (§1) address that.

**The correct mechanism is to filter the index itself before retrieval, not the retrieved output after the fact:** every retrieval call is parameterized by the decision timestamp T, and the query against the store is `WHERE ingested_at <= T` (a metadata filter enforced at the database/vector-index level, e.g., a partition key or a mandatory filtered ANN search), so content with a later knowledge-time is structurally unreachable, not merely disrecommended. Practically, this means: partition or index unstructured content by ingestion-time buckets (daily), and generate/maintain **immutable as-of snapshots** of the retrievable corpus for each historical game used in backtesting, built once and frozen, so a backtest run in 2027 sees exactly the same retrievable universe as one run in 2026 for the same game — this is also what makes the backtest reproducible per §1.

### Detecting leakage after the fact
Point-in-time filtering can still fail silently (a mis-tagged ingestion timestamp, a scraper backfilling a "Tuesday" article with a stale timestamp). Detection layer, independent of the prevention layer:
- **Canary tests** (as in §1): inject known-false facts into the as-of index for spot-checked historical games and confirm the pipeline's output reflects the injected fact, not the real outcome.
- **Ablation deltas by recency:** performance uplift from the LLM/retrieval layer should be roughly stable across the backtest period if it's real; a backtest where the uplift is concentrated in games well before the model's cutoff and vanishes in the most recent pre-cutoff games, or vanishes entirely post-cutoff, is the tell.
- **"Too good" as a red flag, not a result:** if the full pipeline clears 52.38% by a wide, stable margin in backtest but the classical-model-only ablation (no LLM/retrieval features) is close to breakeven, treat the gap as a leakage hypothesis to be disproven, not a discovery to be reported. Genuine, durable sports-betting edges are typically thin — a large gap attributable to the LLM layer specifically should raise the prior on leakage, not on model quality. (Judgment.)
- **Live/paper-trading confirmation is the only real validator.** Per the contamination literature cited in §1, a backtest — however carefully constructed — cannot fully substitute for evaluation on new games generated after any component was frozen; this should be budgeted as a required phase, not treated as optional confirmation of a backtest that already "looks good."

---

## 3. What actually needs retrieving

| Source | What's retrieved | Structured? | Update freq | Latency need | Index/access pattern | Edge plausibility & is retrieval the right pattern? |
|---|---|---|---|---|---|---|
| **Play-by-play, box scores, efficiency metrics (EPA, success rate, DVOA-like)** | Per-play and per-game numeric aggregates | Fully structured | Post-game (final), some near-live in-game | Batch, not real-time for pretrained features | **Plain SQL / feature store, keyed by team-week.** Not a retrieval problem at all. | Backbone of any legitimate model; calling this "RAG" would be architectural fashion — it's a data warehouse. Confidence: High. |
| **Injury reports & depth charts** | NFL: standardized practice-participation + game-status designations. CFB: no NCAA-wide mandate; CFP requires public availability reports starting with the 2025 season; the Big Ten and SEC separately instituted their own conference-level mandates (2024-2025), other conferences vary. | Structured where mandated (NFL, CFP, Big Ten, SEC); unstructured/rumor-based elsewhere (most of the rest of CFB) | NFL: Wed/Thu/Fri practice reports, final designation by 4pm ET Friday. CFB (CFP): availability reports due three days before each game with daily updates and a final report 90 minutes before kickoff. | NFL structured feed: hourly is enough. CFB unstructured (non-mandated portion): as fast as possible given no official cadence. | NFL/CFP/conference-mandated: SQL table. CFB non-mandated portion: genuinely unstructured — the one place hybrid retrieval over recent-news chunks is warranted. | **NFL/CFP/conference-mandated portion: structured lookup, not retrieval.** The remaining non-mandated CFB portion is the one place the NFL-vs-CFB disclosure asymmetry could be a genuine, narrow edge (fragmented info, slower market consensus), but it's noisier/less verified, so it plausibly cuts both ways. Judgment; not verified as real edge here. See source table for citations and a verification-method caveat (several source URLs for this row could not be directly re-fetched in this environment and are corroborated via WebSearch triangulation instead — see Correction Log). |
| **Weather (forecast + actual), especially wind for totals** | Wind speed/direction, precipitation, temperature | Structured | Hourly-to-daily near kickoff | Needs current data near kickoff | **API/DB, not retrieval.** | Well-established structured input for totals; no RAG role. |
| **Betting lines (opening, current, closing, multi-book) + movement timestamps** | Numeric line/price time series | Fully structured | Real-time/tick-level | Precise timestamps required to compute CLV | **Time-series DB, not retrieval.** | Not a retrieval problem — but the single most important feed, since CLV is graded against it. |
| **News, beat-reporter reporting, social signals** | Free text | Unstructured | Continuous | Hours for feature layer; minutes for real-time monitor | **This is the legitimate RAG surface area** — hybrid dense/sparse retrieval over recency-partitioned, as-of-timestamped chunks | Plausible narrow edge only if it surfaces info not yet in structured feeds/line; high false-positive risk. This is what (b)'s feature layer and (c)'s monitor are built for. |
| **Coaching/scheme changes, travel, rest days, altitude, surface** | Mostly structured facts, occasional unstructured commentary | Mostly structured | Low frequency, schedule-driven | Batch, well ahead of kickoff | Structured table + small text-classification pass for scheme narrative | Mostly a database problem; unstructured sliver is a feature-extraction candidate, not real-time monitoring (known weeks ahead). |

**Overall judgment:** the majority of "what needs retrieving" is a data-engineering problem (structured time-series/relational data), not a RAG problem. RAG's legitimate surface area is limited to fragmented, non-mandated-disclosure text (mostly the non-CFP/non-conference-mandated slice of CFB availability chatter) and general news/beat-reporter signal. Presenting this as a "RAG system" end-to-end would over-scope the architecture to match the project's name rather than its data shape.

---

## 4. Retrieval design

- **Corpus scope:** limited to news/beat-reporter text and non-mandated CFB availability chatter — do not build a vector index over structured data; that is fashion, not a requirement.
- **Chunking:** article/post-level, tagged with team+week metadata and mandatory bitemporal fields; short-form corpus, not a generic RAG default.
- **Indexing/retrieval method:** hybrid dense + BM25 with reciprocal rank fusion is current best practice for short, entity-dense text ([digitalapplied.com, 2026](https://www.digitalapplied.com/blog/hybrid-search-bm25-vector-reranking-reference-2026); [atlan.com, accessed 2026](https://atlan.com/know/hybrid-rag/)). Confidence: Medium — engineering-blog sources, not domain-specific peer review. (Not flagged by the audit; not re-verified in this correction round beyond the original pass.)
- **Reranking:** cross-encoder rerank over top-100–1000 fused candidates is standard and cheap; instruction-following rerankers (e.g., Voyage rerank-2.5, Aug 2025) are useful if entity-specific filtering is needed. Confidence: Medium. (Not flagged by the audit; not re-verified in this correction round.)
- **Vector database vs. Postgres/pgvector:** **pgvector (or pgvectorscale for headroom) is sufficient and the right default at this scale.** A Tiger Data-run benchmark reports pgvectorscale reaching 471.57 QPS at 99% recall versus Qdrant's 41.47 QPS on a 50-million-vector, 1536-dimension workload (roughly an 11x throughput advantage for pgvectorscale), though Qdrant showed better per-query p95/p99 latency and a faster index-build time in the same comparison. *Verification note:* the original tigerdata.com URL did not resolve on recheck; the same specific figures are corroborated across independent secondary write-ups of what appears to be the same underlying benchmark — [Medium, Apr 2026](https://medium.com/@anupkawarase.akz/pgvectorscale-beat-qdrant-11-5x-at-50m-vectors-your-vector-db-bill-should-scare-you-c90bbe062499); [Instaclustr, accessed 2026](https://www.instaclustr.com/education/vector-database/pgvector-vs-qdrant-5-key-differences-and-how-to-choose/) — treated as triangulated via WebSearch, not directly fetched from a primary source in this session. This project's news/injury-chatter corpus will be orders of magnitude smaller than 50 million vectors for the foreseeable future, so this benchmark is presented only as an upper-bound sanity check, not as the expected operating point. **The prior draft's specific claim that "pgvector alone matches dedicated DBs below ~10M vectors" rested on a zenvanriel.com post that did not resolve on recheck and could not be re-sourced from any other outlet; that specific crossover figure has been deleted rather than re-cited (see Correction Log).** **Judgment, Low confidence on any precise crossover threshold, Medium confidence on the general direction:** no authoritative source for an exact pgvector-vs-dedicated-vector-DB crossover point was located in this pass; the only source-backed claim is that pgvector/pgvectorscale remains throughput-competitive at tens-of-millions-of-vectors scale, which is already far beyond what this project's corpus needs. **A dedicated vector DB is architectural fashion at this project's actual scale, not a requirement.**
- **Fashion flag, explicit:** agentic multi-hop retrieval, late-interaction multi-vector indexes, or a knowledge-graph layer are disproportionate to this corpus's actual size/shape. Judgment.

---

## 5. Reference architecture

```mermaid
flowchart TB
    subgraph Ingestion["Ingestion (bitemporal, immutable)"]
        A1[Play-by-play / box scores feed]
        A2[NFL official injury feed]
        A3[CFB availability: CFP mandated + conference + scraped beat reports]
        A4[Weather API]
        A5[Multi-book line feed, tick-level]
        A6[News / beat reporter / social scraper]
        A7[Schedule facts: rest, travel, altitude, surface]
    end

    Ingestion --> Snap[Immutable as-of snapshot store\n(event_time, ingested_at, source_snapshot_id)]

    Snap --> Struct[(Structured feature store\nPostgres: EPA/success-rate,\nlines, weather, schedule)]
    Snap --> Unstruct[(Unstructured corpus\nPostgres+pgvector, chunked,\nbitemporal-partitioned)]

    Unstruct --> Retr[Hybrid retrieval\nBM25 + dense + RRF fusion\n+ cross-encoder rerank\nfiltered by as-of timestamp]
    Retr --> Extract[LLM/classifier feature extraction\n(pinned model version, temp=0,\ncached, versioned prompts)]
    Extract --> FeatStore[(Derived feature store\ninjury_severity, sentiment,\nconfidence fields)]

    Struct --> FeatJoin[Feature join / as-of point-in-time join]
    FeatStore --> FeatJoin

    FeatJoin --> Model[Classical predictor\nGBM / hierarchical Bayesian\n+ isotonic/Platt calibration]
    Model --> Pub[Published spread/total]

    Retr -.narrow, bounded.-> Monitor[Near-real-time line-staleness monitor\n(rule-based re-weighting only,\nno LLM-generated number)]
    Monitor -.flags.-> HumanGate[Human review / gated trigger]
    HumanGate -.optional override.-> Pub

    Pub --> Backtest[Backtest / evaluation harness]
    Snap --> Backtest
    Backtest --> Ablation[Ablation: with/without LLM features,\npre-cutoff vs post-cutoff split,\ncanary injection tests]
    Ablation --> Report[CLV-vs-close report, leakage report]
```

**Data flow narrative:** ingestion writes immutable, bitemporally-tagged records for every source. The structured feature store and unstructured corpus are both derived from the same immutable snapshot layer, never a live "current state" table, so any historical prediction can be exactly replayed against the corpus as it existed at that decision time. LLM feature extraction runs in batch against the as-of-filtered retrieval layer, writes versioned features, and joins the structured features on team-week keys. The classical model is the only component emitting the published number. The real-time monitor is a separate, narrow, human-gated path that never bypasses the model.

**Evaluation/backtest harness as a first-class component:** requires (1) the frozen as-of snapshot for each historical game's actual decision timestamp, (2) pinned model/prompt versions for that period, (3) mandatory ablation runs (with/without LLM features), (4) mandatory pre-cutoff/post-cutoff split reporting, (5) canary-injection test results, and (6) CLV computed against the multi-book line feed, not a single closing number. No backtest result should be reported without items 3-5 attached.

---

## 6. Where this conflicts with the edge research

1. **The real-time monitor (§1c) assumes sportsbooks are measurably slow to reprice public unstructured information** relative to how fast this pipeline can act. If books reprice within seconds using their own scraping infrastructure, this component contributes zero CLV and is pure engineering cost — unverified here; the architecture is built so it can be A/B tested (with/without, measure CLV delta) rather than assumed.
2. **The CFB non-mandated-disclosure asymmetry (§3) requires assuming CFB markets are less efficient at pricing fragmented injury information than NFL markets.** Plausible-sounding, but exactly the kind of argument that turns out to already be priced by syndicate-informed lines. This memo takes no position; it only argues the feature is cheap enough to build and ablate in isolation.
3. **Recommending (b)/(c) over (a) implicitly assumes any marginal edge is a few percentage points at most**, consistent with efficient-market priors. If the edge research finds evidence of a large, qualitative-synthesis-shaped inefficiency (making holistic LLM reasoning itself the edge, not just a feature), this architecture under-invests in the reasoning layer and §1's recommendation should be revisited, not treated as settled.
4. **The leakage-detection methodology (§2) assumes a stable, non-contamination-explained margin over 52.38% is statistically observable** within a reasonable post-cutoff evaluation window. If any true edge is thin enough to be statistically indistinguishable from noise over that sample, the ablation methodology may lack power to separate "small real edge" from "no edge" from "residual leakage" — a sample-size/power problem outside this memo's scope, but it limits how much confidence this design can produce even if built correctly.

---

## Open questions

1. What historical depth of unstructured news/injury data is actually scrapeable for CFB far enough back to build a meaningful post-cutoff-excluded backtest sample, given the CFB disclosure asymmetry is recent (CFP mandate starts 2025-26, Big Ten/SEC mandates only slightly earlier)?
2. Which pinned LLM/classifier will be used for feature extraction, and what is its actual public training cutoff — this determines how many historical seasons are backtest-usable versus contaminated?
3. Does the business want the real-time line-staleness monitor in v1, given §6 flags its value as unverified and its engineering cost is nontrivial?
4. What is the actual expected query/write volume for the unstructured corpus over a multi-season horizon — needed to confirm the pgvector-suffices judgment in §4 stays valid?
5. Who validates precision/recall of the LLM feature-extraction step against ground truth (e.g., human-labeled injury severity), and how often?
6. This correction round could not directly WebFetch any external domain (network egress was blocked for every domain attempted, including arxiv.org, collegefootballplayoff.com, cbssports.com, and operations.nfl.com). Before this memo is used to justify spend, someone with unblocked network access should directly re-verify the handful of claims still resting on WebSearch triangulation rather than a direct primary-document read (flagged inline and in the Correction Log below).

---

## Source table

| Source | Date | What it supports | URL |
|---|---|---|---|
| ForecastBench: A Dynamic Benchmark of AI Forecasting Capabilities (Karger, Bastani, et al.) | submitted Sep 2024; published as a conference paper at ICLR 2025 | LLM forecasting Brier scores: superforecasters 0.093, general public 0.107, best LLM (Claude 3.5 Sonnet) 0.111. Verified via WebSearch triangulation across 3 independent queries (arXiv mirrors, EA Forum announcement, ResearchGate, ICLR slides); direct WebFetch to arxiv.org was blocked in this environment. | https://arxiv.org/abs/2409.19839 |
| Future Is Unevenly Distributed: Forecasting Ability of LLMs Depends on What We're Asking (Karkar & Chopra, Lossfunk) | submitted Nov 23, 2025 | Small independent paper; qualitative finding that LLM forecasting calibration is domain/prompt-dependent and degrades when contextual news is injected — cited only for this qualitative point, not for specific scores. Verified via WebSearch triangulation (arXiv abstract, Hugging Face papers page, ResearchGate, secondary summaries); direct WebFetch blocked in this environment. | https://arxiv.org/abs/2511.18394 |
| Do Large Language Models Know What They Don't Know? KalshiBench: A New Benchmark for Evaluating Epistemic Calibration via Prediction Markets | Dec 2025 | LLM overconfidence at high-probability levels. Not flagged by the audit; not re-verified beyond the original pass in this correction round. | https://arxiv.org/pdf/2512.16030 |
| Temporal Leakage in LLM Backtesting: Measurement, Validation, and Adjusted Scores | Aug 2026 | Contamination mechanism/detection for LLM backtests. Not flagged by the audit; not re-verified beyond the original pass in this correction round. | https://arxiv.org/html/2608.02985 |
| All Leaks Count, Some Count More: Interpretable Temporal Contamination Detection and Mitigation | Feb 2026 | Naive before/after-cutoff check unreliable; ablation needed. Not flagged by the audit; not re-verified beyond the original pass in this correction round. | https://arxiv.org/html/2602.17234 |
| Thinking Machines Lab, "Defeating Nondeterminism in LLM Inference" | Sep 10, 2025 | Primary source for batch-invariance nondeterminism at temp 0: 80/1000 completions diverged under standard kernels; identical under batch-invariant kernels; ~1.6-2x inference-time cost for determinism. Replaces a previously miscited LMSYS/SGLang URL and an unsupported "9% accuracy swing" figure — see Correction Log. | https://thinkingmachines.ai/blog/defeating-nondeterminism-in-llm-inference/ |
| Simon Willison, "Defeating Nondeterminism in LLM Inference" (commentary) | Sep 11, 2025 | Independent corroborating summary of the Thinking Machines Lab finding. | https://simonwillison.net/2025/Sep/11/defeating-nondeterminism/ |
| Defeating nondeterminism in LLM inference, in practice (Medium) | 2025 | Practical summary of batch-invariance findings. | https://medium.com/@siddhantg314/defeating-nondeterminism-in-llm-inference-in-practice-38a7dd1e4112 |
| NFL Important Dates 2025-2026 (official PDF) | Jun 5, 2025 | NFL injury-report cadence/season dates. Not flagged by the audit. | https://media.nfl.com/content/dam/communications/football-communications/2025/news/06%2005%2025%20-%20Important%20Dates.pdf |
| CBS Sports, "How to decipher the NFL injury report" | accessed 2026 | NFL Wed/Thu/Fri report cadence, Friday 4pm ET final designation, Out/Doubtful/Questionable/practice-participation categories. Replaces a previously cited profootballnetwork.com URL that did not resolve on recheck. Verified via WebSearch; direct WebFetch to cbssports.com blocked in this environment. | https://www.cbssports.com/fantasy/football/news/how-to-decipher-the-nfl-injury-report/ |
| NFL Football Operations, injury (personnel) report policy documents | 2016/2017 versions located | Official league-source description of practice-participation levels and designation categories; cited as the underlying policy source corroborating the CBS Sports summary above. Direct WebFetch to operations.nfl.com blocked in this environment; existence and content corroborated via WebSearch only. | https://operations.nfl.com/media/2683/2017-nfl-injury-report-policy.pdf |
| Sportshandle, "NCAA Doesn't Plan To Mandate College Football Injury Reports" | 2019 stance, article accessed 2025-2026 | No NCAA-wide mandate; older underlying stance but still operative. Not flagged by the audit; not re-verified beyond the original pass. | https://sportshandle.com/ncaa-college-football-injury-report-discussions/ |
| CBS Sports, "College Football Playoff will require teams to provide player availability reports beginning with 2025 season" | 2025 | CFP-specific mandated disclosure starting 2025 season. Corroborated via independent secondary sources found in this correction round (SI.com "Big change coming to college football with player reports in 2025"; hoosierstateofmind.com; NCAA Division I FBS Oversight Committee report, Dec 2025). Direct WebFetch to cbssports.com blocked in this environment; verified via WebSearch triangulation only. | https://www.cbssports.com/college-football/news/college-football-playoff-will-require-teams-to-provide-player-availability-reports-beginning-with-2025-season/ |
| College Football Playoff, Student-Athlete Availability Reporting Policy v3 (official PDF) | Dec 16, 2025 | CFP availability-report cadence specifics: reports due 3 days before each game, daily updates, final report 90 minutes before kickoff, escalating fines for inaccurate/late reporting. Replaces a previously cited collegefootballplayoff.com/reports.aspx URL that did not resolve on recheck; this newer, more specific policy-document URL was located during this correction round but likewise could not be directly WebFetched (blocked in this environment) — verified via WebSearch triangulation only. | https://collegefootballplayoff.com/documents/2025/12/16/Student-Athlete_Availability_Reporting_Policy_v3.pdf |
| CBS Sports, "SEC institutes player availability injury reports for all teams during 2024 college football season" | 2024 | SEC conference-level mandate, predating the CFP-wide mandate. Replaces a previously cited sportshandle.com/college-football-injury-reporting-conference-decision URL that did not resolve on recheck. Verified via WebSearch triangulation only. | https://www.cbssports.com/college-football/news/sec-institutes-player-availability-injury-reports-for-all-teams-during-2024-college-football-season/ |
| hoosierstateofmind.com, "Following the SEC and Big Ten, the College Football Playoff will now require teams to report player availability" | accessed 2026 | Big Ten conference-level mandate details (reports due  2 hours before kickoff; questionable/out designations). Corroborating source added in this correction round. Verified via WebSearch triangulation only. | https://hoosierstateofmind.com/following-the-sec-and-big-ten-the-college-football-playoff-will-now-require-teams-to-report-player-availability-01jycspqeaey |
| Tiger Data-run pgvectorscale vs. Qdrant benchmark (figures corroborated via secondary write-ups) | benchmark referenced 2025, secondary write-ups Apr 2026 / accessed 2026 | pgvectorscale ~471.57 QPS vs. Qdrant ~41.47 QPS at 99% recall, 50M vectors, 1536-dim; Qdrant better on p95/p99 latency and index-build time. Original tigerdata.com URL did not resolve on recheck; figures triangulated across two independent secondary write-ups instead. | https://medium.com/@anupkawarase.akz/pgvectorscale-beat-qdrant-11-5x-at-50m-vectors-your-vector-db-bill-should-scare-you-c90bbe062499 ; https://www.instaclustr.com/education/vector-database/pgvector-vs-qdrant-5-key-differences-and-how-to-choose/ |
| ~~zenvanriel.com, "pgvector vs Dedicated Vector Databases"~~ | — | **Removed.** Previously cited for a "~10M vectors" pgvector-vs-dedicated-DB crossover claim. URL did not resolve on recheck and no alternate source for that specific figure was located; the claim has been deleted from §4, not re-cited (see Correction Log). | (removed — no authority located) |
| digitalapplied.com, "Hybrid Search: BM25, Vector & Reranking Reference 2026" | 2026 | Hybrid retrieval + reranking as default practice. Not flagged by the audit; not re-verified beyond the original pass. | https://www.digitalapplied.com/blog/hybrid-search-bm25-vector-reranking-reference-2026 |
| atlan.com, "Hybrid RAG: Dense and Sparse Retrieval for Better AI Answers" | accessed 2026 | Rationale for hybrid retrieval on short-form text. Not flagged by the audit; not re-verified beyond the original pass. | https://atlan.com/know/hybrid-rag/ |

**Sourcing gaps (explicit):** No dedicated, peer-reviewed study of LLM-generated point-spread/totals calibration (as opposed to general-event probability forecasting) was found; the numeric-calibration argument in §1 extrapolates from general forecasting-calibration literature and is labeled judgment. **Every citation touched in this correction round was verified via WebSearch cross-triangulation (multiple independent queries, checked for consistency across differently-authored secondary sources) rather than a single direct WebFetch of a primary document** — this environment's network egress proxy blocked direct WebFetch to every external domain attempted (arxiv.org, huggingface.co, semanticscholar.org, cbssports.com, collegefootballplayoff.com, operations.nfl.com, emergentmind.com, forum.effectivealtruism.org, faculty.wharton.upenn.edu). Where triangulation across independent sources was strong and consistent, the claim is retained with Medium confidence and the verification method is disclosed inline. Where no independent corroboration could be found for a specific figure (the pgvector "~10M vector" crossover claim; the original "9% accuracy swing" figure), the specific figure was deleted rather than re-cited. Anyone with unblocked network access should re-verify these before they are used to justify spend — see Open Question 6.

---

## Correction Log

This section records the corrections made in response to the manager's citation audit. Route key: **(a)** = replaced with a verified real source; **(b)** = weakened/reframed to match what evidence actually supports, marked judgment/lower confidence; **(c)** = deleted, no authority located.

1. **ForecastBench Brier-score claim (§1, load-bearing EDGE finding flagged by name).** The prior draft stated frontier models reach Brier scores "~0.135–0.159," a human crowd of "~0.149," and superforecasters of "~0.02" — none of which match the actual ForecastBench paper and could not be sourced to a single stable URL. **Route (a):** located and cited the actual paper (arXiv:2409.19839, Karger/Bastani et al., ICLR 2025) and replaced the figures with what it actually reports: superforecasters 0.093, general public 0.107, best LLM tested (Claude 3.5 Sonnet) 0.111. This is a *stronger* point for the memo's own recommendation (the best 2024-era LLM underperformed even an ordinary crowd, not just superforecasters), so the correction does not weaken the argument for rejecting RAG-as-predictor — it strengthens it. Also narrowed the claim to the specific models ForecastBench tested and removed the unsupported claim that o3/o4-mini-class models were shown reaching this range. Confidence set to Medium (verified via WebSearch triangulation across independent sources; direct WebFetch to arxiv.org was blocked in this environment).
2. **"Future Is Unevenly Distributed" (arXiv:2511.18394).** Re-examined and found to be a two-author independent paper (Karkar & Chopra, Lossfunk), not a large benchmark result. **Route (b):** reframed to cite it only for its qualitative claim (context-dependent calibration failure modes), removed any implication it corroborates specific Brier-score bands, and lowered confidence to Medium-Low.
3. **Determinism section: LMSYS/SGLang URL and "9% accuracy swings" figure.** The lmsys.org URL failed to resolve, and the specific "9%" figure could not be traced to any source in this or the prior research pass. **Route (a) + (c) hybrid:** replaced the citation with the actual primary source (Thinking Machines Lab's blog post) and replaced the unsupported "9%" figure with the specific, verifiable finding actually reported there (80/1000 completions diverged under standard kernels; identical under batch-invariant kernels; ~1.6-2x inference-time cost for determinism).
4. **pgvector vs. Qdrant benchmark citation (tigerdata.com).** URL failed to resolve. **Route (a):** replaced with two independent secondary write-ups reporting the same underlying benchmark figures (471.57 vs. 41.47 QPS at 50M vectors/99% recall); figures unchanged since they were corroborated consistently, but the primary Tiger Data blog itself is no longer cited as directly verified.
5. **pgvector-vs-dedicated-DB "~10M vectors" crossover claim (zenvanriel.com).** URL failed to resolve and no alternate source with that specific threshold was found. **Route (c):** deleted the specific figure. Replaced with an explicitly labeled judgment statement (Low confidence on any precise threshold, Medium confidence on the general "competitive at tens-of-millions-of-vectors scale" direction, based on the surviving pgvectorscale-vs-Qdrant benchmark).
6. **CFP player-availability mandate and reporting cadence (collegefootballplayoff.com/reports.aspx; CBS Sports mandate article).** Both URLs failed to resolve. **Route (b)/(a) hybrid:** kept the underlying claim, since it is corroborated by multiple additional independent sources found in this round (SI.com, hoosierstateofmind.com, an NCAA D-I FBS Oversight Committee report), and located a more specific, newer official CFP policy PDF (Dec 16, 2025) as the primary citation for the cadence detail. All of these remain verified only via WebSearch triangulation (direct WebFetch blocked in this environment for both collegefootballplayoff.com and cbssports.com), so confidence is disclosed inline as triangulation-only, not directly fetched.
7. **Conference-level (Big Ten/SEC) injury-disclosure mandate citation (sportshandle.com/college-football-injury-reporting-conference-decision).** URL failed to resolve. **Route (a):** replaced with two directly-located corroborating sources (CBS Sports SEC-mandate article; hoosierstateofmind.com Big Ten article), same underlying claim, substance unchanged.
8. **NFL injury-report cadence/policy citation (profootballnetwork.com).** URL failed to resolve. **Route (a):** replaced with a CBS Sports explainer article and the NFL's own operations.nfl.com policy PDF, same underlying claim (Wed/Thu/Fri cadence, Friday 4pm ET deadline, Out/Doubtful/Questionable designations), substance unchanged.

**Cross-cutting methodological note:** in this correction round, WebFetch (direct document retrieval) was blocked by this environment's network egress policy for every external domain attempted — including arxiv.org, huggingface.co, semanticscholar.org, cbssports.com, collegefootballplayoff.com, operations.nfl.com, emergentmind.com, forum.effectivealtruism.org, and faculty.wharton.upenn.edu. All corrections above were therefore verified via WebSearch cross-triangulation (checking that specific facts/figures appear consistently across multiple, independently-authored sources returned by separate search queries) rather than a single direct primary-document read. This is disclosed inline at each affected claim and flagged as Open Question 6 for follow-up by someone with unblocked network access before these specific figures are relied on for a spend decision.
